const fs = require('fs');
const path = require('path');
const glob = require('glob'); // Not available by default, I'll use simple recursive search or grep_search.

console.log("I'll use grep_search");
